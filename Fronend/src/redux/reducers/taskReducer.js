const initialState = {
    tasks: [],
    sharedTasks: [],
};

const taskReducer = (state = initialState, action) => {
    switch (action.type) {
        // Task reducer logic
        default:
            return state;
    }
};

export default taskReducer;
