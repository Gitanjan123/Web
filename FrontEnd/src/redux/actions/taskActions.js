import axios from 'axios';

export const getTasks = () => async (dispatch) => {
    // Get tasks action logic
};

export const createTask = (taskData) => async (dispatch) => {
    // Create task action logic
};

export const updateTask = (taskId, taskData) => async (dispatch) => {
    // Update task action logic
};

export const deleteTask = (taskId) => async (dispatch) => {
    // Delete task action logic
};

export const getSharedTasks = () => async (dispatch) => {
    // Get shared tasks action logic
};
